import type { IngredientGroup } from '../types';
import { orange, apple, banana, grape } from './ingredientsMock';

export const fruitGroup: IngredientGroup = {
	name: 'Fruit',
	ingredients: [orange, apple, banana, grape]
};
